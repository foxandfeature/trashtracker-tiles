{
	"layers": {
		"bins": { "minzoom": 10, "maxzoom": 14 }
	},
	"settings": {
		"minzoom": 10,
		"maxzoom": 14,
		"basezoom": 14,
		"include_ids": false,
		"name": "TrashTracker bins",
		"version": "0.1",
		"description": "Public waste baskets (OSM amenity=waste_basket) worldwide, visible from zoom 10.",
		"compress": "gzip"
	}
}
